int so1 = 10;
int so2 = 20;
int so3 = 15;
int so4 = 25;
int so5 = 100;
int so6 = 200;

int tinhTong(int soA, int soB)
{
    return soA + soB;
}

Console.WriteLine("Tong so1 va so2: " + tinhTong(so1, so2));
Console.WriteLine("Tong so3 va so4: " + tinhTong(so3, so4));
Console.WriteLine("Tong so5 va so6: " + tinhTong(so5, so6));